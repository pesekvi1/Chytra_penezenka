@model DataAccess.Model.PolozkaRozpoctu
@{
    Layout = null;
}

<tr>
    <td>@Html.DisplayFor(x => x.Ucel)</td>
    <td>@Html.DisplayFor(x => x.Cena)</td>
    <td>
        <a class="icon-link" href="@Url.Action("Odstranit", "Polozka", new {id = Model.Id, rozpocetId = ViewBag.RozpocetId})" onclick="return confirm('Přejete si opravdu odebrat polozku?')">
            <span class="fas fa-lg fa-trash-alt icon-red"></span>
        </a>
    </td>
</tr>
